import { accentColors } from '../../config';
import { Color } from '../../types';

export const sectionTitle = 'Validation Data';
export const colorName: Color = 'green';
export const primaryColor = accentColors[colorName];
