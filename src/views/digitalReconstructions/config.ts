import { accentColors } from '../../config';
import { Color } from '../../types';

export const sectionTitle = 'Digital Reconstructions';
export const color: Color = 'lavender';
export const primaryColor = accentColors[color];
