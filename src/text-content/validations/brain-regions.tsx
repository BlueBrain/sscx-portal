import { TextContent } from '@/types';


const textContent: TextContent = {
  head: {
    title: 'Brain regions / Validations / SSCx Portal',
    description: 'We provide validations of the network activity in the SSCx reconstruction by comparing against relevant experimental data.',
  },
};


export default textContent;
