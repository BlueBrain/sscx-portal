import { TextContent } from '@/types';


const textContent: TextContent = {
  head: {
    title: 'Home / SSCx Portal',
    description: 'The SSCx portal is a freely accessible resource, which provides access to the Blue Brain Project\'s experimental data sets on the multi-scale organization of the juvenile rat primary somatosensory cortex - sub-regions, microcircuits, neurons, synapses, and the resulting computational models.',
  },
};


export default textContent;
