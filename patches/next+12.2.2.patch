diff --git a/node_modules/next/dist/server/font-utils.js b/node_modules/next/dist/server/font-utils.js
index 574697d..2c61561 100755
--- a/node_modules/next/dist/server/font-utils.js
+++ b/node_modules/next/dist/server/font-utils.js
@@ -46,6 +46,8 @@ function _interopRequireWildcard(obj) {
     return newObj;
 }
 const https = require("https");
+const UrlLib = require('url');
+const HttpsProxyAgent = require('https-proxy-agent');
 const CHROME_UA = "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_5) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/83.0.4103.61 Safari/537.36";
 const IE_UA = "Mozilla/5.0 (Windows NT 10.0; Trident/7.0; rv:11.0) like Gecko";
 function isGoogleFont(url) {
@@ -54,11 +56,15 @@ function isGoogleFont(url) {
 function getFontForUA(url, UA) {
     return new Promise((resolve, reject)=>{
         let rawData = "";
-        https.get(url, {
-            headers: {
-                "user-agent": UA
-            }
-        }, (res)=>{
+        let options = UrlLib.parse(url);
+        options.headers={
+          'user-agent': UA
+        };
+        if (options.protocol === 'https:' && process.env.http_proxy) {
+          let agent = new HttpsProxyAgent(process.env.http_proxy);
+          options.agent = agent;
+        }
+        https.get(options, (res)=>{
             res.on("data", (chunk)=>{
                 rawData += chunk;
             });
